import React, { Component } from 'react'

export default class PlayArea extends Component {
    render() {
        return (
            <div id="map">
                <canvas id="mapCanvas"/>
            </div>
        )
    }
}
